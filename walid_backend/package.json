{
  "name": "walid-backend",
  "version": "1.0.0",
  "description": "Backend module for journalisation, statistics and alerts for the administrative dashboard.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint . --ext .ts",
    "start": "npm run build && firebase emulators:start --only functions",
    "test": "jest --runInBand",
    "deploy": "npm run build && firebase deploy --only functions,firestore:rules,firestore:indexes"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-admin": "^11.10.0",
    "firebase-functions": "^4.5.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/jest": "^29.2.5",
    "@types/node": "^20.5.7",
    "eslint": "^8.46.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-prettier": "^4.2.1",
    "firebase-functions-test": "^3.0.2",
    "jest": "^29.7.0",
    "prettier": "^3.0.0",
    "supertest": "^7.1.4",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.3"
  }
}